# 02 · 整体架构

## 分层架构图

```mermaid
graph TB
    subgraph Clients["MCP 客户端(Claude Code / Cursor / …)"]
        A1["带 Token 的 URL 或 Bearer 头"]
    end
    subgraph Browser["管理员浏览器"]
        B1["控制台 SPA"]
    end

    NGINX["宿主机 Nginx(HTTPS 终止,X-Forwarded-For)"]

    subgraph Gateway["网关进程(单 uvicorn,单端口 8000)"]
        QMW["QueryTokenAuthMiddleware<br/>(最外层:?token= → Authorization 头)"]
        MCP["/mcp · FastMCP Streamable HTTP"]
        AUTH["DbTokenVerifier<br/>(SHA-256 查 access_tokens)"]
        GMW["GatewayMiddleware<br/>(tier 门禁 / 工具白名单 / RPM / 日配额 / 月 Credits)"]
        EXEC["PooledExecutor<br/>(选 key + 故障转移 + research 轮询 + 记日志)"]
        POOL["KeyPool(内存状态机 + SQLite 持久化)"]
        ALERTS["Alerter(告警队列)<br/>飞书/企微/钉钉/Webhook/邮件"]
        ADMIN["/api/* · 管理 REST API(25 路由)<br/>(签名 Cookie 会话)"]
        TASKS["后台任务:6h 配额校准 / 日志清理<br/>+ alerts-sender 告警发送"]
        METRICS["/metrics · Prometheus"]
        DB[("SQLite(WAL)<br/>tavily_keys · access_tokens<br/>request_logs · settings")]
        STATIC["/ · 控制台静态文件(StaticFiles)"]
    end

    TAVILY["Tavily API<br/>search / extract / crawl / map / research / usage"]

    A1 --> NGINX --> QMW --> MCP
    B1 --> NGINX --> ADMIN
    PROM["Prometheus 抓取"] -.-> NGINX --> METRICS
    MCP --> AUTH --> GMW --> EXEC
    EXEC --> POOL
    EXEC -- "httpx" --> TAVILY
    POOL -- "状态变化" --> ALERTS
    TASKS --> ALERTS
    EXEC -- "计数" --> METRICS
    POOL --> DB
    EXEC --> DB
    ADMIN --> DB
    ALERTS --> DB
    TASKS -- "GET /usage 校准" --> TAVILY
    TASKS --> DB
    NGINX --> STATIC
```

## 一次工具调用的完整流转

```mermaid
sequenceDiagram
    participant C as MCP 客户端
    participant Q as QueryTokenAuth
    participant V as DbTokenVerifier
    participant M as GatewayMiddleware
    participant E as PooledExecutor
    participant P as KeyPool
    participant T as Tavily

    C->>Q: POST /mcp?token=tpm_xxx (tools/call)
    Q->>Q: 无 Authorization 头则注入 Bearer
    Q->>V: 验证 Token(SHA-256 查库,吊销即时生效)
    V-->>M: AccessToken(claims: token_id/tier/allowed_tools/限额)
    M->>M: crawl/map/research 需 full 等级 → 工具白名单 → RPM → 日/月配额
    M->>E: 放行 on_call_tool
    E->>P: acquire() 轮询取可用 key
    E->>T: 调用上游(携带该 key)
    alt 成功(同步端点)
        T-->>E: 200 + usage.credits + request_id
        E->>P: report_success(累计 credits,超额自动标耗尽 + 触发告警)
        E->>E: 写 request_logs(success + 查询词 + IP + 延迟)+ Prometheus 计数
    else research(异步)
        T-->>E: 201 + request_id(status=pending)
        loop 固定该 key 轮询,直至 completed / failed / 超时
            E->>T: GET /research/{request_id}
        end
    else 429 / 432 / 401 / 5xx
        E->>P: 冷却 / 标耗尽 / 禁用 / 仅记错误(均触发告警评估)
        E->>T: 换下一个 key 重试(≤ min(key数, 4))
    else 4xx 参数错误
        E-->>C: 直接透传错误(换 key 也一样,不重试)
    end
    E-->>C: 工具结果(格式化 + 25k 字符截断)
```

## 为什么是"单进程单端口"

顶层 ASGI 应用直接用 `mcp.http_app(path="/mcp")` 的返回值,再把管理路由、健康检查、静态文件**追加**到它的 `routes` 列表。这是 FastMCP 官方推荐的 ASGI 集成方式,关键原因:

- 若把 MCP app **Mount** 进自建 Starlette,子应用的 lifespan 不会执行,MCP 会话管理器无法启动
- 本项目把自定义 lifespan 传给 **FastMCP 构造器**(`FastMCP(lifespan=...)`),由 `http_app()` 的 lifespan 统一驱动,数据库连接、KeyPool 加载、后台任务都在同一个生命周期里

组装顺序见 `app/main.py:create_app()`:

```
mcp.http_app(path="/mcp")          # 路由表首项是 /mcp
  .routes += /health               # 健康检查(无鉴权)
  .routes += /metrics              # Prometheus 指标(无鉴权,只有聚合数值)
  .routes += build_admin_routes()  # 25 条 /api/* 与公开的 /site-icon、/api/public-info
  .routes += Mount("/", StaticFiles(dashboard/dist))  # 静态托管放最后兜底
最后整体包一层 QueryTokenAuthMiddleware 返回
```

> **实测教训**:`?token=` 提升中间件最初通过 `http_app(middleware=[...])` 注入,但 FastMCP 的 `AuthenticationMiddleware` 位于用户中间件**外层**、更早读取 Authorization 头,导致注入无效。因此该中间件必须包裹在**整个 app 最外层**(见 `main.py` 末尾注释)。

## 启动流程(lifespan)

1. `load_config()`:读环境变量;`ADMIN_PASSWORD` 必填(或 dev 模式默认 `admin`);`SESSION_SECRET` 缺省时自动生成并持久化到 `DATA_DIR/session_secret`
2. `db.connect()`:打开 SQLite、WAL、busy_timeout,执行 `SCHEMA` 建表,`_migrate()` 补列(`token_enc`、`allowed_tools`、`client_ip`、`query`)
3. `pool.load()`:全量加载 `tavily_keys` 到内存
4. 启动两个后台任务:`usage-sync`(每 6h 校准全部 key)、`maintenance`(每 6h 月度滚动 + 删过期日志);两者每轮结束都调 `alerter.pool_check()` 评估池健康
5. `alerter.start()`:起 `alerts-sender` 后台任务消费告警队列(webhook/邮件异步发送)
6. 开始服务;关闭时反向清理(取消任务 → 关 httpx → 关 alerter → 关 DB)

## 部署拓扑(当前生产环境)

```mermaid
graph LR
    DEV["开发者 push main"] --> GH["GitHub Actions<br/>deploy.yml"]
    GH -->|"job1: build"| TCR["腾讯云 TCR<br/>ccr.ccs.tencentyun.com/yjkz/tavily-map<br/>(:sha + :latest)"]
    GH -->|"job2: deploy"| SRV["服务器(宝塔)<br/>scp deploy/docker-compose.yml →<br/>ssh: docker compose pull && up -d"]
    USER["用户/客户端"] -->|"HTTPS"| NGX["宿主 Nginx"] -->|"127.0.0.1:8000"| APP["tavily-map 容器"]
```

- CI 需要 4 个 secrets:`DOCKER_PASSWORD`、`SERVER_HOST`、`SERVER_USER`、`SERVER_SSH_KEY`
- 两个 job 均绑定 GitHub Environment `腾讯云4h4g`(可用于审批/环境密钥)
- 容器仅绑定 `127.0.0.1:${APP_PORT:-8000}`,公网只暴露 Nginx 的 443
